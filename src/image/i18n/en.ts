import { defineMessages } from "@trebired/i18n";

export default defineMessages({
    "encode-failed": "Failed to encode image",
    "invalid-buffer": "Invalid image buffer",
    "missing-upload-dir": "Upload folder is missing",
    "unknown-image-upload-kind": "Unknown image upload kind",
    "upload-dir-not-absolute": "Upload folder must be an absolute path",
    "write-failed": "Failed to write image to disk",
});
